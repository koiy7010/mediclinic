spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/mediclinic}

logging:
  level:
    com.smartguys: INFO
    root: WARN

server:
  port: ${SERVER_PORT:8080}

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:3000}
